{
  "name": "weld-for-wp",
  "version": "0.3.0",
  "private": true,
  "description": "Cardano wallet connectivity and transaction signing for WordPress.",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
